name: Binder Badge
on:
  pull_request_target:
    types: [opened]

jobs:
  binder:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    uses: ./.github/actions/binder-link
    with:
      github_token: ${{secrets.GITHUB_TOKEN}}
